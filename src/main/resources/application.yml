spring:
  application:
    name: service-registry

server:
  port: 8761
eureka:
  instance:
    prefer-ip-address: true
    hostname: localhost
  client:
    fetch-registry: false
    register-with-eureka: false


